# 2026-06-30 11:30 Codex Update

## What Changed

- Added the CVCI model checkpoint Baidu Netdisk download link and extraction code to `README.md`.
- Documented the expected default checkpoint path, `weights/iter_25000.pth`, alongside the download link.

## Why It Changed

- Users need a visible source for the model checkpoint because the repository intentionally does not include weight files.

## Commands/Tests Run

- `git status --short`
- `rg --files -g 'README*' -g 'readme*' -g 'docs/**'`
- `sed -n '1,260p' README.md`

No code tests were required because this is a documentation-only change.

## GPU Used

- No GPU was used. No GPU workload was run.

## Files Changed

- `README.md`
- `docs/codex_changes/20260630_1130_codex_update.md`

## Remaining Risks or TODOs

- Existing unrelated working-tree changes were present before this update: `rule_change_log.md` is deleted and `README.md` already had uncommitted edits. This update only intends to add the model checkpoint download information.
